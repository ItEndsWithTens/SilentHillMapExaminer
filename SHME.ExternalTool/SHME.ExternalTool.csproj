<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <LangVersion>8.0</LangVersion>
    <Nullable>enable</Nullable>
    <TargetFramework>net48</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.NETFramework.ReferenceAssemblies" Version="1.0.0" PrivateAssets="all" />
    <PackageReference Include="OpenTK" Version="3.0.1" />
    <PackageReference Include="OpenTK.GLControl" Version="3.0.1" />
    <Reference Include="System.Windows.Forms" />
    <Reference Include="BizHawk.Client.Common" HintPath="$(ProjectDir)../lib/BizHawk/dll/BizHawk.Client.Common.dll" />
    <Reference Include="BizHawk.Client.EmuHawk" HintPath="$(ProjectDir)../lib/BizHawk/EmuHawk.exe" />
    <Reference Include="BizHawk.Common" HintPath="$(ProjectDir)../lib/BizHawk/dll/BizHawk.Common.dll" />
    <Reference Include="BizHawk.WinForms.Controls" HintPath="$(ProjectDir)../lib/BizHawk/dll/BizHawk.WinForms.Controls.dll" />
  </ItemGroup>

  <Target Name="PostBuild" AfterTargets="PostBuildEvent">
    <Copy SourceFiles="$(OutputPath)$(MSBuildProjectName).dll" DestinationFolder="$(ProjectDir)../lib/BizHawk/ExternalTools" />
  </Target>
</Project>
